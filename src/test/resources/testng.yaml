name: "All"
verbose: 2
tests:
  - name: Behaviour.DifferenceSets
    packages:
      - name: hitucc.behaviour.differenceSets

  - name: Behaviour.Dictionary
    packages:
      - name: hitucc.behaviour.dictionary

  - name: Model
    packages:
      - name: hitucc.model

  - name: Performance
    packages:
      - name: hitucc.performance