name: "All"
verbose: 2
tests:
  - name: Behaviour.DifferenceSets
    packages:
      - name: hit_ucc.behaviour.differenceSets

  - name: Behaviour.Dictionary
    packages:
      - name: hit_ucc.behaviour.dictionary

  - name: Model
    packages:
      - name: hit_ucc.model

  - name: Performance
    packages:
      - name: hit_ucc.performance